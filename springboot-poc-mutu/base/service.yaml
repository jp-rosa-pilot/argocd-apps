apiVersion: v1
kind: Service
metadata:
  name: springboot-poc-mutu
  labels:
    app: springboot-poc-mutu  
spec:
  selector:
    app: springboot-poc-mutu
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: api
